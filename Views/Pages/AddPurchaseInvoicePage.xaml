<base:BaseAnimatedPage
    x:Class="PrimeAppBooks.Views.Pages.AddPurchaseInvoicePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:PrimeAppBooks.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="clr-namespace:PrimeAppBooks.ViewModels.Pages"
    xmlns:converters="clr-namespace:PrimeAppBooks.Conveters"
    d:DataContext="{d:DesignInstance Type=pages:AddPurchaseInvoicePageViewModel}"
    d:DesignHeight="900"
    d:DesignWidth="1100"
    Background="Transparent"
    mc:Ignorable="d">

    <base:BaseAnimatedPage.Resources>
        <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />
    </base:BaseAnimatedPage.Resources>

    <ScrollViewer Style="{StaticResource ModernScrollViewer}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="86*" />
                <RowDefinition Height="159*"/>
            </Grid.RowDefinitions>

            <!-- Header Section -->
            <Border Grid.Row="0" Margin="10,0,10,10" Style="{StaticResource ModernCardStyle}" Width="1035">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Style="{StaticResource WelcomeTitleText}" Text="{Binding PageTitle}" />
                        <TextBlock Style="{StaticResource WelcomeSubtitleText}" Text="Record and post a bill from your vendor to your ledger" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button
                            Margin="5,0"
                            Command="{Binding NavigateBackCommand}"
                            Content="← Back"
                            Style="{StaticResource ModernSecondaryButton}" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Form -->
            <Grid Margin="0,125,0,0" Grid.RowSpan="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Invoice Details Header -->
                <Border Grid.Row="0" Margin="0,0,0,15" Style="{StaticResource ModernCardStyle}" Width="1040" HorizontalAlignment="Left">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Invoice Number -->
                        <StackPanel Grid.Column="0" Margin="5">
                            <TextBlock Style="{StaticResource DetailLabelText}" Text="Bill #" />
                            <TextBox Style="{StaticResource ModernTextBoxStyle}" Text="{Binding InvoiceNumber}" Width="210" />
                        </StackPanel>

                        <!-- Vendor Selection -->
                        <StackPanel Grid.Column="1" Margin="5">
                            <TextBlock Style="{StaticResource DetailLabelText}" Text="Vendor" />
                            <ComboBox
                                DisplayMemberPath="VendorName"
                                ItemsSource="{Binding Vendors}"
                                SelectedItem="{Binding SelectedVendor}"
                                Style="{StaticResource ModernComboBox}" Width="210" />
                        </StackPanel>

                        <!-- Dates -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <StackPanel Margin="5">
                                <TextBlock Style="{StaticResource DetailLabelText}" Text="Date" />
                                <DatePicker SelectedDate="{Binding InvoiceDate}" Style="{StaticResource CompactDatePickerStyle}" Width="110"/>
                            </StackPanel>
                            <StackPanel Margin="5">
                                <TextBlock Style="{StaticResource DetailLabelText}" Text="Due Date" />
                                <DatePicker SelectedDate="{Binding DueDate}" Style="{StaticResource CompactDatePickerStyle}" Width="110"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Multi-Currency -->
                        <StackPanel Grid.Column="3" Orientation="Horizontal">
                            <StackPanel Margin="5">
                                <TextBlock Style="{StaticResource DetailLabelText}" Text="Currency" />
                                <ComboBox
                                    Width="90"
                                    DisplayMemberPath="CurrencyCode"
                                    ItemsSource="{Binding Currencies}"
                                    SelectedItem="{Binding SelectedCurrency}"
                                    Style="{StaticResource ModernComboBox}" />
                            </StackPanel>
                            <StackPanel Margin="5">
                                <TextBlock Style="{StaticResource DetailLabelText}" Text="Ex. Rate" />
                                <hc:TextBox
                                    Width="80"
                                    hc:InfoElement.Placeholder="1.00"
                                    Style="{StaticResource ModernTextBoxStyle}"
                                    Text="{Binding ExchangeRate, StringFormat=N4, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Line Items Grid -->
                <Border Grid.Row="1" Margin="0,0,0,15" Style="{StaticResource ModernCardStyle}" Width="1040" HorizontalAlignment="Left">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Margin="5" Style="{StaticResource SectionHeaderText}" Text="Line Items" />

                        <DataGrid
                            Grid.Row="1"
                            MaxHeight="400"
                            Margin="5"
                            AutoGenerateColumns="False"
                            BorderThickness="0"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            GridLinesVisibility="None"
                            IsReadOnly="False"
                            ItemsSource="{Binding BillLines}"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                            ScrollViewer.VerticalScrollBarVisibility="Auto"
                            Style="{StaticResource ModernDataGridStyle}">

                            <DataGrid.RowStyle>
                                <Style BasedOn="{StaticResource MaterialDesignDataGridRow}" TargetType="DataGridRow">
                                    <Setter Property="Height" Value="60" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="Foreground" Value="White" />
                                    <Setter Property="Background" Value="Transparent" />
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#2A4A7B" />
                                            <Setter Property="BorderBrush" Value="#4472C4" />
                                            <Setter Property="BorderThickness" Value="1" />
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#1E3A5F" />
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>

                            <DataGrid.CellStyle>
                                <Style BasedOn="{StaticResource MaterialDesignDataGridCell}" TargetType="DataGridCell">
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="Transparent" />
                                            <Setter Property="BorderBrush" Value="Transparent" />
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>

                            <DataGrid.ColumnHeaderStyle>
                                <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="DataGridColumnHeader">
                                    <Setter Property="Foreground" Value="#FF4472C4" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="FontFamily" Value="Segoe UI" />
                                    <Setter Property="FontSize" Value="12" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Height" Value="50" />
                                    <Setter Property="Background" Value="#1E1E1E" />
                                    <Setter Property="BorderBrush" Value="#3A3A3A" />
                                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                                </Style>
                            </DataGrid.ColumnHeaderStyle>

                            <DataGrid.Columns>
                                <DataGridTextColumn
                                    Width="Auto"
                                    Binding="{Binding LineNumber}"
                                    Header="#"
                                    IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center" />
                                            <Setter Property="HorizontalAlignment" Value="Center" />
                                            <Setter Property="Foreground" Value="#7F8C8D" />
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!-- Item / Product Selection (optional - for inventory items) -->
                                <DataGridTemplateColumn Header="Item / Product" Width="180">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox
                                                DisplayMemberPath="ItemName"
                                                ItemsSource="{Binding DataContext.InventoryItems, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                SelectedItem="{Binding SelectedItem, UpdateSourceTrigger=PropertyChanged}"
                                                Style="{StaticResource ModernComboBox}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Header="Account" Width="180">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox
                                                DisplayMemberPath="FullName"
                                                ItemsSource="{Binding DataContext.Accounts, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                SelectedItem="{Binding SelectedAccount, UpdateSourceTrigger=PropertyChanged}"
                                                Style="{StaticResource ModernComboBox}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="Description" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="Auto" />
                                <DataGridTextColumn Header="Qty" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="70" />
                                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Width="100" />
                                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=N2}" IsReadOnly="True" Width="Auto" />
                                <DataGridTemplateColumn Header="Action" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button
                                                Command="{Binding DataContext.RemoveLineCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource ModernIconButton}"
                                                ToolTip="Remove Line">
                                                <Path
                                                    Width="12"
                                                    Height="12"
                                                    Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z"
                                                    Fill="#E74C3C"
                                                    Stretch="Uniform" />
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <StackPanel Grid.Row="0" HorizontalAlignment="Right">
                            <Button
                                Margin="5"
                                Command="{Binding AddLineCommand}"
                                Content="➕ Add Line"
                                Style="{StaticResource ModernSecondaryButton}" />
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Footer / Totals -->
                <Grid Grid.Row="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Margin="0,0,10,0" Style="{StaticResource ModernCardStyle}" Width="750" HorizontalAlignment="Left">
                            <StackPanel Margin="10">
                                <TextBlock Style="{StaticResource DetailLabelText}" Text="Notes" />
                                <TextBox
                                    AcceptsReturn="True"
                                    Height="60"
                                    Style="{StaticResource ModernTextBoxStyle}"
                                    Text="{Binding Notes}"
                                    TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" MinWidth="250" Style="{StaticResource ModernCardStyle}" Width="280">
                            <StackPanel Margin="20">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Style="{StaticResource SectionHeaderText}" Text="Total:" />
                                    <TextBlock Style="{StaticResource WelcomeTitleText}" Text="{Binding TotalAmount, StringFormat=C}" Foreground="#FFFF6B6B" />
                                </StackPanel>
                                <Grid Margin="0,20,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Button
                                        Grid.Column="0"
                                        Margin="0,0,5,0"
                                        Command="{Binding SaveDraftCommand}"
                                        Content="💾 Save Draft"
                                        Style="{StaticResource ModernSecondaryButton}" />
                                    <Button
                                        Grid.Column="1"
                                        Margin="5,0,0,0"
                                        Command="{Binding SaveAndPostCommand}"
                                        Content="🚀 Record &amp; Post"
                                        Style="{StaticResource ModernActionButton}" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Validation Summary Footer -->
                    <Border Grid.Row="1" Margin="0,10,0,0" Padding="15" CornerRadius="8" Background="#20FFFFFF" Width="1040">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Style="{StaticResource SectionHeaderText}" Text="📋 Validation Status" FontSize="14" Margin="0,0,0,10" />
                                <ItemsControl ItemsSource="{Binding ValidationErrors}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Foreground="#E74C3C" Text="{Binding}" Margin="0,2" FontSize="12" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <TextBlock Visibility="{Binding HasValidationErrors, Converter={StaticResource InverseBooleanToVisibilityConverter}}" 
                                           Foreground="#2ECC71" Text="✓ All entries are valid and ready to post." FontSize="12" Margin="0,5" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>

            <!-- Loading Overlay -->
            <!--<Border
                Grid.RowSpan="5"
                Background="#80000000"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar
                        Width="80"
                        Height="80"
                        IsIndeterminate="True"
                        Style="{StaticResource MaterialDesignCircularProgressBar}" />
                    <TextBlock
                        Margin="0,15,0,0"
                        HorizontalAlignment="Center"
                        Style="{StaticResource DetailValueText}"
                        Text="Processing..." />
                </StackPanel>
            </Border>-->
        </Grid>
    </ScrollViewer>
</base:BaseAnimatedPage>