<base:BaseAnimatedPage
    x:Class="PrimeAppBooks.Views.Pages.PayablesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:PrimeAppBooks.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:PrimeAppBooks.ViewModels.Pages"
    d:DataContext="{d:DesignInstance Type=vm:PayablesPageViewModel}"
    d:DesignHeight="900"
    d:DesignWidth="1280"
    Background="Transparent"
    mc:Ignorable="d">

    <ScrollViewer Style="{StaticResource ModernScrollViewer}">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Header  -->
            <Border Grid.Row="0" Margin="5,10" Style="{StaticResource ModernCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Button
                        Grid.Column="0"
                        Height="Auto"
                        Margin="0,0,15,0"
                        VerticalAlignment="Center"
                        Command="{Binding GoBackCommand}"
                        Content="◀"
                        FontSize="16"
                        Foreground="White"
                        Style="{StaticResource ModernIconButton}"
                        ToolTip="Go back" />

                    <StackPanel Grid.Column="1">
                        <TextBlock Style="{StaticResource WelcomeTitleText}" Text="Payables Summary and Vendor Aging" />
                        <TextBlock Style="{StaticResource WelcomeSubtitleText}" Text="Monitor accounts payable, vendor balances, and payment schedules" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Margin="5,0" Command="{Binding CreatePurchaseInvoiceCommand}" Content="📝 New Bill" Style="{StaticResource ModernActionButton}" />
                    </StackPanel>
                </Grid>
            </Border>

            <!--  KPI Cards  -->
            <UniformGrid Grid.Row="1" Margin="5,0" Columns="3">
                <Border Margin="3,0" Style="{StaticResource ModernCardStyle}">
                    <StackPanel Style="{StaticResource CardContentContainer}">
                        <TextBlock FontSize="14" Foreground="#7F8C8D" Text="Total Payables" />
                        <TextBlock Style="{StaticResource CardValueText}" Text="{Binding TotalPayables, StringFormat=C}" />
                        <TextBlock Style="{StaticResource CardSubText}" Text="Outstanding vendor liability" />
                    </StackPanel>
                </Border>

                <Border Margin="3,0" Style="{StaticResource ModernCardStyle}">
                    <StackPanel Style="{StaticResource CardContentContainer}">
                        <TextBlock FontSize="14" Foreground="#FF6B6B" Text="Total Past Due" />
                        <TextBlock Foreground="#FF6B6B" Style="{StaticResource CardValueText}" Text="{Binding TotalOverdue, StringFormat=C}" />
                        <TextBlock Style="{StaticResource CardSubText}" Text="Past bill due date" />
                    </StackPanel>
                </Border>

                <Border Margin="3,0" Style="{StaticResource ModernCardStyle}">
                    <StackPanel Style="{StaticResource CardContentContainer}">
                        <TextBlock FontSize="14" Foreground="#2ECC71" Text="Vendor Count" />
                        <TextBlock Foreground="#2ECC71" Style="{StaticResource CardValueText}" Text="{Binding VendorList.Count}" />
                        <TextBlock Style="{StaticResource CardSubText}" Text="Vendors with balances" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!--  Charts  -->
            <Grid Grid.Row="2">
                <Border Margin="5,10" Style="{StaticResource ModernCardStyle}">
                    <StackPanel>
                        <TextBlock Margin="0,0,0,15" Style="{StaticResource SectionHeaderText}" Text="Payables Aging Report" />
                        <lvc:CartesianChart Height="250" LegendLocation="None" Series="{Binding AgingSeries}">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Days Since Bill Date" Labels="{Binding AgingLabels}" />
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Amount" />
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </StackPanel>
                </Border>
            </Grid>

            <!--  Detailed List  -->
            <Grid Grid.Row="3">
                <Border Margin="5,0" Style="{StaticResource ModernCardStyle}">
                    <DockPanel>
                        <Grid DockPanel.Dock="Top" Margin="0,0,0,15">
                            <TextBlock Style="{StaticResource SectionHeaderText}" Text="Vendor Liability Analysis" />
                            <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#7F8C8D" Text="{Binding ResultsSummary}" />
                        </Grid>

                        <DataGrid
                            MaxHeight="400"
                            HorizontalAlignment="Stretch"
                            AutoGenerateColumns="False"
                            BorderThickness="0"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            GridLinesVisibility="None"
                            IsReadOnly="True"
                            ItemsSource="{Binding VendorList}"
                            Style="{StaticResource ModernDataGridStyle}">

                            <DataGrid.RowStyle>
                                <Style BasedOn="{StaticResource MaterialDesignDataGridRow}" TargetType="DataGridRow">
                                    <Setter Property="Height" Value="55" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="Background" Value="Transparent" />
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#2A4A7B" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#1E3A5F" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>

                            <DataGrid.ColumnHeaderStyle>
                                <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="DataGridColumnHeader">
                                    <Setter Property="Foreground" Value="#FF4472C4" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="Background" Value="#1E1E1E" />
                                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                                </Style>
                            </DataGrid.ColumnHeaderStyle>

                            <DataGrid.Columns>
                                <DataGridTextColumn Width="*" Binding="{Binding VendorName}" Header="Vendor / Provider" />
                                <DataGridTextColumn Width="Auto" Binding="{Binding TotalOutstanding, StringFormat=C}" Header="Total Payable" />
                                <DataGridTextColumn Width="Auto" Binding="{Binding OverdueAmount, StringFormat=C}" Header="Overdue">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#FF6B6B" />
                                            <Setter Property="FontWeight" Value="SemiBold" />
                                            <Setter Property="VerticalAlignment" Value="Center" />
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTemplateColumn Width="Auto" Header="Action">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button
                                                Command="{Binding DataContext.ViewVendorDetailsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Content="Manage"
                                                Style="{StaticResource ModernMiniButton}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</base:BaseAnimatedPage>