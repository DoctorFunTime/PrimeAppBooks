<Page x:Class="PrimeAppBooks.Views.Pages.Audit"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:converters="clr-namespace:PrimeAppBooks.Conveters"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:hc="https://handyorg.github.io/handycontrol"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      d:DesignHeight="900"
      d:DesignWidth="1280"
      Background="Transparent"
      mc:Ignorable="d">

    <ScrollViewer Style="{StaticResource ModernScrollViewer}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header Section -->
            <Border Grid.Row="0" Margin="0,0,0,20" Style="{StaticResource ModernCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Style="{StaticResource WelcomeTitleText}" Text="Audit Trail" />
                        <TextBlock Style="{StaticResource WelcomeSubtitleText}" Text="Monitor all system activities and changes for compliance and security" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button
                            Margin="5,0"
                            Command="{Binding ExportAuditLogCommand}"
                            Content="📥 Export Log"
                            Style="{StaticResource ModernActionButton}" />
                        <Button
                            Margin="5,0"
                            Command="{Binding PurgeOldRecordsCommand}"
                            Content="🧹 Cleanup"
                            Style="{StaticResource ModernSecondaryButton}" />
                        <Button
                            Margin="5,0"
                            Command="{Binding RealTimeMonitoringCommand}"
                            Content="{Binding MonitoringStatus, Converter={StaticResource MonitorStatusConverter}}"
                            Style="{StaticResource ModernSecondaryButton}" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Audit Summary Cards -->
            <UniformGrid Grid.Row="1" Columns="5" Margin="0,0,0,20">
                <!-- Total Activities -->
                <Border Style="{StaticResource ModernCardStyle}">
                    <StackPanel Style="{StaticResource CardContentContainer}">
                        <TextBlock Style="{StaticResource DraftHeaderText}" Text="📊 Total Activities" />
                        <TextBlock Style="{StaticResource CardValueText}" Text="{Binding TotalActivities}" />
                        <TextBlock Style="{StaticResource CardSubText}" Text="All time" />
                    </StackPanel>
                </Border>

                <!-- Today's Activities -->
                <Border Style="{StaticResource ModernCardStyle}">
                    <StackPanel Style="{StaticResource CardContentContainer}">
                        <TextBlock Style="{StaticResource PostedHeaderText}" Text="📈 Today" />
                        <TextBlock Style="{StaticResource CardValueText}" Text="{Binding TodaysActivities}" />
                        <TextBlock Style="{StaticResource CardSubText}" Text="Activities today" />
                    </StackPanel>
                </Border>

                <!-- Critical Events -->
                <Border Style="{StaticResource ModernCardStyle}">
                    <StackPanel Style="{StaticResource CardContentContainer}">
                        <TextBlock Style="{StaticResource MonthlyHeaderText}" Text="⚠️ Critical" />
                        <TextBlock Style="{StaticResource CardValueText}" Text="{Binding CriticalEvents}" />
                        <TextBlock Style="{StaticResource CardSubText}" Text="Require attention" />
                    </StackPanel>
                </Border>

                <!-- User Activities -->
                <Border Style="{StaticResource ModernCardStyle}">
                    <StackPanel Style="{StaticResource CardContentContainer}">
                        <TextBlock Style="{StaticResource BalanceHeaderText}" Text="👥 Active Users" />
                        <TextBlock Style="{StaticResource CardValueText}" Text="{Binding ActiveUsers}" />
                        <TextBlock Style="{StaticResource CardSubText}" Text="Recent activity" />
                    </StackPanel>
                </Border>

                <!-- Data Changes -->
                <Border Style="{StaticResource ModernCardStyle}">
                    <StackPanel Style="{StaticResource CardContentContainer}">
                        <TextBlock Style="{StaticResource BalanceHeaderText}" Text="🔄 Data Changes" />
                        <TextBlock Style="{StaticResource CardValueText}" Text="{Binding DataChanges}" />
                        <TextBlock Style="{StaticResource CardSubText}" Text="Modifications" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Main Content Area -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="400" />
                </Grid.ColumnDefinitions>

                <!-- Audit Log Table -->
                <Border Grid.Column="0" Margin="0,0,15,0" Style="{StaticResource ModernCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!-- Filter Header -->
                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Style="{StaticResource SectionHeaderText}"
                                Text="📋 Audit Log" />

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <ComboBox
                                    Width="120"
                                    Margin="0,0,10,0"
                                    ItemsSource="{Binding ActionTypeFilters}"
                                    SelectedItem="{Binding SelectedActionType}"
                                    Style="{StaticResource ModernComboBox}" />
                                <Button
                                    Command="{Binding RefreshCommand}"
                                    Content="🔄"
                                    Style="{StaticResource ModernRefreshButton}" />
                            </StackPanel>
                        </Grid>

                        <!-- Quick Filters -->
                        <Border Grid.Row="1" Margin="0,0,0,15" Style="{StaticResource DetailInfoBorder}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    Margin="0,0,10,0"
                                    VerticalAlignment="Center"
                                    Style="{StaticResource FilterLabelText}"
                                    Text="Quick filters:" />

                                <ToggleButton
                                    Content="Critical Only"
                                    IsChecked="{Binding ShowCriticalOnly}"
                                    Style="{StaticResource FilterToggleButton}" />
                                <ToggleButton
                                    Margin="5,0"
                                    Content="Data Changes"
                                    IsChecked="{Binding ShowDataChangesOnly}"
                                    Style="{StaticResource FilterToggleButton}" />
                                <ToggleButton
                                    Content="User Logins"
                                    IsChecked="{Binding ShowLoginsOnly}"
                                    Style="{StaticResource FilterToggleButton}" />

                                <hc:TextBox
                                    Width="200"
                                    Margin="10,0,0,0"
                                    hc:InfoElement.Placeholder="Search audit trail..."
                                    Style="{StaticResource HandyModernTextBox}"
                                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                            </StackPanel>
                        </Border>

                        <!-- Audit DataGrid -->
                        <Border Grid.Row="2" Style="{StaticResource ModernDataGridContainer}">
                            <DataGrid
                                AutoGenerateColumns="False"
                                ItemsSource="{Binding AuditEntries}"
                                SelectedItem="{Binding SelectedAuditEntry}"
                                Style="{StaticResource ModernDataGridStyle}">

                                <DataGrid.Columns>
                                    <!-- Severity Indicator -->
                                    <DataGridTemplateColumn Width="50">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="{Binding Severity, Converter={StaticResource AuditActionToColorConverter}}"
                                                        Style="{StaticResource AuditSeverityIndicator}" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <!-- Timestamp -->
                                    <DataGridTextColumn
                                        Width="150"
                                        Binding="{Binding Timestamp, StringFormat='MMM dd, yyyy HH:mm'}"
                                        Header="Time" />

                                    <!-- User -->
                                    <DataGridTextColumn
                                        Width="120"
                                        Binding="{Binding Username}"
                                        Header="User" />

                                    <!-- Action Type -->
                                    <DataGridTextColumn
                                        Width="120"
                                        Binding="{Binding ActionType}"
                                        Header="Action" />

                                    <!-- Description -->
                                    <DataGridTextColumn
                                        Width="*"
                                        MinWidth="200"
                                        Binding="{Binding Description}"
                                        Header="Description" />

                                    <!-- Entity Type -->
                                    <DataGridTextColumn
                                        Width="120"
                                        Binding="{Binding EntityType}"
                                        Header="Entity" />

                                    <!-- Entity ID -->
                                    <DataGridTextColumn
                                        Width="100"
                                        Binding="{Binding EntityId}"
                                        Header="Entity ID" />

                                    <!-- IP Address -->
                                    <DataGridTextColumn
                                        Width="120"
                                        Binding="{Binding IpAddress}"
                                        Header="IP Address" />

                                    <!-- Status -->
                                    <DataGridTemplateColumn Width="80" Header="Status">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="{Binding Status, Converter={StaticResource StatusColorConverter}}"
                                                        Style="{StaticResource TransactionStatusBadge}">
                                                    <TextBlock Style="{StaticResource StatusBadgeText}" Text="{Binding Status}" />
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </Grid>
                </Border>

                <!-- Audit Detail Panel -->
                <Border Grid.Column="1" Style="{StaticResource ModernCardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionHeaderText}" Text="🔍 Audit Details" />

                        <StackPanel Margin="0,15,0,0" Visibility="{Binding SelectedAuditEntry, Converter={StaticResource NullToVisibilityConverter}}">
                            <!-- Basic Info -->
                            <Border Style="{StaticResource DetailInfoBorder}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource DetailHeaderText}" Text="{Binding SelectedAuditEntry.ActionType}" />
                                    <TextBlock Style="{StaticResource DetailSubText}" Text="{Binding SelectedAuditEntry.Timestamp, StringFormat='MMM dd, yyyy HH:mm:ss'}" />

                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <Border Background="{Binding SelectedAuditEntry.Severity, Converter={StaticResource AuditActionToColorConverter}}"
                                                Style="{StaticResource TransactionStatusBadge}">
                                            <TextBlock Style="{StaticResource StatusBadgeText}" Text="{Binding SelectedAuditEntry.Severity}" />
                                        </Border>
                                        <TextBlock
                                            Margin="10,0,0,0"
                                            Style="{StaticResource DetailSubText}"
                                            Text="{Binding SelectedAuditEntry.Username}" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Changes Made -->
                            <Border Style="{StaticResource DetailInfoBorder}" Visibility="{Binding SelectedAuditEntry.HasChanges, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource DetailLabelText}" Text="Changes Made" />
                                    <ItemsControl ItemsSource="{Binding SelectedAuditEntry.Changes}" Margin="0,5,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="0,0,0,5" Background="#2A2A2A" Padding="8" CornerRadius="4">
                                                    <StackPanel>
                                                        <TextBlock Style="{StaticResource DetailValueText}" Text="{Binding PropertyName}" />
                                                        <Grid Margin="0,2,0,0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" Text="{Binding OldValue}" Foreground="#E74C3C" FontSize="11" />
                                                            <TextBlock Grid.Column="1" Text="{Binding NewValue}" Foreground="#27AE60" FontSize="11" />
                                                        </Grid>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <!-- Additional Context -->
                            <Border Style="{StaticResource DetailInfoBorder}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource DetailLabelText}" Text="Additional Context" />
                                    <TextBlock Style="{StaticResource DetailValueText}" Text="{Binding SelectedAuditEntry.Description}" TextWrapping="Wrap" />

                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <TextBlock Style="{StaticResource DetailSubText}" Text="IP: " />
                                        <TextBlock Style="{StaticResource DetailValueText}" Text="{Binding SelectedAuditEntry.IpAddress}" />
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Style="{StaticResource DetailSubText}" Text="Entity: " />
                                        <TextBlock Style="{StaticResource DetailValueText}" Text="{Binding SelectedAuditEntry.EntityType}" />
                                        <TextBlock Style="{StaticResource DetailSubText}" Text=" ID: " />
                                        <TextBlock Style="{StaticResource DetailValueText}" Text="{Binding SelectedAuditEntry.EntityId}" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Actions -->
                            <StackPanel Margin="0,10,0,0">
                                <Button
                                    Command="{Binding InvestigateCommand}"
                                    CommandParameter="{Binding SelectedAuditEntry}"
                                    Content="🔍 Investigate"
                                    Style="{StaticResource ModernSecondaryButton}"
                                    Margin="0,0,0,5" />
                                <Button
                                    Command="{Binding ExportEntryCommand}"
                                    CommandParameter="{Binding SelectedAuditEntry}"
                                    Content="📄 Export Entry"
                                    Style="{StaticResource ModernSecondaryButton}" />
                            </StackPanel>
                        </StackPanel>

                        <!-- Empty State -->
                        <StackPanel
                            Margin="0,20,0,0"
                            HorizontalAlignment="Center"
                            Visibility="{Binding SelectedAuditEntry, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}">
                            <TextBlock
                                Margin="0,0,0,10"
                                HorizontalAlignment="Center"
                                FontSize="48"
                                Opacity="0.3"
                                Text="🔍" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                Style="{StaticResource DetailSubText}"
                                Text="Select an audit entry to view details" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>