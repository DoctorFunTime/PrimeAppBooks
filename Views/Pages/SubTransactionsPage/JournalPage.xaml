<base:BaseAnimatedPage
    x:Class="PrimeAppBooks.Views.Pages.SubTransactionsPage.JournalPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:PrimeAppBooks.Views.Pages"
    xmlns:converters="clr-namespace:PrimeAppBooks.Conveters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fw="clr-namespace:SourceChord.FluentWPF;assembly=FluentWPF"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:PrimeAppBooks.Views.Pages.SubTransactionsPage"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:subtransactionspage="clr-namespace:PrimeAppBooks.ViewModels.Pages.SubTransactionsPage"
    Title="Journal Entry"
    d:DataContext="{d:DesignInstance Type=subtransactionspage:JournalPageViewModel}"
    d:DesignHeight="900"
    d:DesignWidth="1280"
    Background="Transparent"
    mc:Ignorable="d">

    <base:BaseAnimatedPage.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </base:BaseAnimatedPage.Resources>

    <ScrollViewer Style="{StaticResource ModernScrollViewer}">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Header Section with Title and Actions  -->
            <Border
                Grid.Row="0"
                Margin="0,0,0,20"
                Style="{StaticResource ModernCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <!--  Back button  -->
                        <ColumnDefinition Width="*" />
                        <!--  Title  -->
                        <ColumnDefinition Width="Auto" />
                        <!--  Action buttons  -->
                    </Grid.ColumnDefinitions>

                    <Button
                        Grid.Column="0"
                        Height="Auto"
                        Margin="0,0,15,0"
                        Command="{Binding GoBackCommand}"
                        Content="◀"
                        FontSize="16"
                        Foreground="White"
                        IsEnabled="{Binding CanGoBack}"
                        Style="{StaticResource ModernIconButton}"
                        ToolTip="Go back" />

                    <StackPanel Grid.Column="1">
                        <TextBlock>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource WelcomeTitleText}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                            <Setter Property="Text" Value="Edit Journal Entry" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsEditing}" Value="False">
                                            <Setter Property="Text" Value="Create Journal Entry" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock>
                            <TextBlock.Style>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource WelcomeSubtitleText}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                            <Setter Property="Text" Value="Modify existing journal entry - save as draft to update" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsEditing}" Value="False">
                                            <Setter Property="Text" Value="Record debits and credits with proper double-entry bookkeeping" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button
                            Margin="5,0"
                            Command="{Binding SaveDraftCommand}"
                            Content="💾 Save Draft"
                            Style="{StaticResource ModernSecondaryButton}" />
                        <Button
                            Margin="5,0"
                            Command="{Binding PostEntryCommand}"
                            Content="✅ Post Entry"
                            IsEnabled="{Binding CanPost}"
                            Style="{StaticResource ModernActionButton}" />
                        <Button
                            Margin="5,0"
                            Command="{Binding CancelCommand}"
                            Content="❌ Cancel"
                            Style="{StaticResource ModernSecondaryButton}" />
                    </StackPanel>
                </Grid>
            </Border>

            <!--  Entry Header Information  -->
            <Border
                Grid.Row="1"
                Margin="0,0,0,20"
                Style="{StaticResource ModernCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  Entry Description  -->
                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                        <TextBlock Style="{StaticResource FilterLabelText}" Text="Entry Description *" />
                        <hc:TextBox
                            Margin="0,5,0,0"
                            Style="{StaticResource ModernTextBoxStyle}"
                            Text="{Binding EntryDescription, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock
                            Margin="0,2,0,0"
                            FontSize="11"
                            Foreground="#E74C3C"
                            Text="{Binding DescriptionValidationError}"
                            Visibility="{Binding HasDescriptionError, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </StackPanel>

                    <!--  Reference Number  -->
                    <StackPanel Grid.Column="1" Margin="0,0,15,0">
                        <TextBlock Style="{StaticResource FilterLabelText}" Text="Reference Number" />
                        <hc:TextBox
                            Margin="0,5,0,0"
                            Style="{StaticResource ModernTextBoxStyle}"
                            Text="{Binding ReferenceNumber, UpdateSourceTrigger=PropertyChanged}" />
                        <TextBlock
                            Margin="0,2,0,0"
                            FontSize="10"
                            Foreground="#95A5A6"
                            Text="Auto-generated if empty" />
                    </StackPanel>

                    <!--  Entry Date  -->
                    <StackPanel Grid.Column="2" Margin="0,0,15,0">
                        <TextBlock Style="{StaticResource FilterLabelText}" Text="Entry Date *" />
                        <DatePicker
                            Margin="0,5,0,0"
                            SelectedDate="{Binding EntryDate}"
                            Style="{StaticResource CompactDatePickerStyle}" />
                        <TextBlock
                            Margin="0,2,0,0"
                            FontSize="11"
                            Foreground="#E74C3C"
                            Text="{Binding DateValidationError}"
                            Visibility="{Binding HasDateError, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </StackPanel>
                </Grid>
            </Border>

            <!--  Main Content Area - Journal Lines  -->
            <Grid Grid.Row="2" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  Journal Lines Grid  -->
                <Border
                    Grid.Column="0"
                    Margin="0,0,10,0"
                    Style="{StaticResource ModernCardStyle}">
                    <StackPanel>
                        <!--  Lines Header  -->
                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Style="{StaticResource SectionHeaderText}"
                                Text="📋 Journal Lines" />

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <Button
                                    Command="{Binding AddLineCommand}"
                                    Content="➕ Add Line"
                                    Style="{StaticResource ModernSecondaryButton}" />
                            </StackPanel>
                        </Grid>

                        <!--  Journal Lines DataGrid  -->
                        <DataGrid
                            MaxHeight="400"
                            HorizontalAlignment="Stretch"
                            AutoGenerateColumns="False"
                            BorderThickness="0"
                            CanUserAddRows="False"
                            CanUserDeleteRows="False"
                            GridLinesVisibility="None"
                            ItemsSource="{Binding JournalLines}"
                            ScrollViewer.HorizontalScrollBarVisibility="Auto"
                            ScrollViewer.VerticalScrollBarVisibility="Auto"
                            SelectedItem="{Binding SelectedLine}"
                            SelectionMode="Single"
                            Style="{StaticResource ModernDataGridStyle}">

                            <!--  Set row height and vertical alignment with enhanced selection state  -->
                            <DataGrid.RowStyle>
                                <Style BasedOn="{StaticResource MaterialDesignDataGridRow}" TargetType="DataGridRow">
                                    <Setter Property="Height" Value="60" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="Foreground" Value="White" />
                                    <Setter Property="Background" Value="Transparent" />
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#2A4A7B" />
                                            <!--  Selected background color  -->
                                            <Setter Property="BorderBrush" Value="#4472C4" />
                                            <Setter Property="BorderThickness" Value="1" />
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#1E3A5F" />
                                            <!--  Hover color  -->
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>

                            <!--  Enhanced cell styling with selection state  -->
                            <DataGrid.CellStyle>
                                <Style BasedOn="{StaticResource MaterialDesignDataGridCell}" TargetType="DataGridCell">
                                    <Setter Property="VerticalAlignment" Value="Center" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                    <Style.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="Transparent" />
                                            <!--  Let row handle background  -->
                                            <Setter Property="BorderBrush" Value="Transparent" />
                                            <Setter Property="Foreground" Value="White" />
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.CellStyle>

                            <!--  Update column header style  -->
                            <DataGrid.ColumnHeaderStyle>
                                <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="DataGridColumnHeader">
                                    <Setter Property="Foreground" Value="#FF4472C4" />
                                    <Setter Property="FontWeight" Value="Bold" />
                                    <Setter Property="FontFamily" Value="Segoe UI" />
                                    <Setter Property="FontSize" Value="12" />
                                    <Setter Property="VerticalContentAlignment" Value="Center" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Height" Value="35" />
                                    <Setter Property="Background" Value="#1E1E1E" />
                                    <Setter Property="BorderBrush" Value="#3A3A3A" />
                                    <Setter Property="BorderThickness" Value="0,0,0,1" />
                                </Style>
                            </DataGrid.ColumnHeaderStyle>

                            <DataGrid.Columns>
                                <!--  Line Number  -->
                                <DataGridTextColumn
                                    Width="Auto"
                                    Binding="{Binding LineNumber}"
                                    Header="#"
                                    IsReadOnly="True">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="VerticalAlignment" Value="Center" />
                                            <Setter Property="HorizontalAlignment" Value="Center" />
                                            <Setter Property="FontWeight" Value="Medium" />
                                            <Setter Property="FontFamily" Value="Segoe UI" />
                                            <Setter Property="Foreground" Value="#7F8C8D" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}" Value="True">
                                                    <Setter Property="Foreground" Value="White" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>

                                <!--  Account Selection  -->
                                <DataGridTemplateColumn Width="200" Header="Account *">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox
                                                VerticalAlignment="Center"
                                                DisplayMemberPath="FullName"
                                                ItemsSource="{Binding DataContext.AvailableAccounts, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                SelectedValue="{Binding AccountId, UpdateSourceTrigger=PropertyChanged}"
                                                SelectedValuePath="AccountId"
                                                Style="{StaticResource ModernComboBox}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!--  Debit Amount  -->
                                <DataGridTemplateColumn Width="Auto" Header="Debit">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <hc:TextBox
                                                HorizontalAlignment="Stretch"
                                                VerticalAlignment="Center"
                                                Style="{StaticResource ModernTextBoxStyle}"
                                                Text="{Binding DebitAmount, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!--  Credit Amount  -->
                                <DataGridTemplateColumn Width="Auto" Header="Credit">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <hc:TextBox
                                                HorizontalAlignment="Stretch"
                                                VerticalAlignment="Center"
                                                Style="{StaticResource ModernTextBoxStyle}"
                                                Text="{Binding CreditAmount, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <!--  Actions  -->
                                <DataGridTemplateColumn Width="Auto" Header="Actions">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                                                <Button
                                                    Command="{Binding DataContext.RemoveLineCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource ModernIconButton}"
                                                    ToolTip="Remove Line">
                                                    <Path
                                                        Width="12"
                                                        Height="12"
                                                        Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z"
                                                        Fill="#E74C3C"
                                                        Stretch="Uniform" />
                                                </Button>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!--  Balance Summary  -->
                        <Border
                            Margin="0,15,0,0"
                            Padding="15"
                            Background="#3A506B"
                            CornerRadius="8">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsBalanced}" Value="True">
                                            <Setter Property="Background" Value="#27AE60" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsBalanced}" Value="False">
                                            <Setter Property="Background" Value="#E74C3C" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                    <TextBlock Style="{StaticResource FilterLabelText}" Text="Total Debits" />
                                    <TextBlock
                                        FontSize="16"
                                        FontWeight="SemiBold"
                                        Foreground="White"
                                        Text="{Binding TotalDebits, StringFormat=C}" />
                                </StackPanel>

                                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                    <TextBlock Style="{StaticResource FilterLabelText}" Text="Total Credits" />
                                    <TextBlock
                                        FontSize="16"
                                        FontWeight="SemiBold"
                                        Foreground="White"
                                        Text="{Binding TotalCredits, StringFormat=C}" />
                                </StackPanel>

                                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                    <TextBlock Style="{StaticResource FilterLabelText}" Text="Difference" />
                                    <TextBlock
                                        FontSize="16"
                                        FontWeight="SemiBold"
                                        Foreground="{Binding DifferenceColor}"
                                        Text="{Binding Difference, StringFormat=C}" />
                                </StackPanel>

                                <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                                    <TextBlock Style="{StaticResource FilterLabelText}" Text="Balance Status" />
                                    <TextBlock
                                        FontSize="16"
                                        FontWeight="SemiBold"
                                        Foreground="White"
                                        Text="{Binding BalanceStatus}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                        <!--  Validation Summary  -->
                        <Border
                            Margin="0,10"
                            Style="{StaticResource DetailInfoBorder}"
                            Visibility="{Binding HasValidationErrors, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock
                                    Foreground="PaleVioletRed"
                                    Style="{StaticResource DetailLabelText}"
                                    Text="⚠️ Validation Issues" />
                                <ItemsControl Margin="0,5,0,0" ItemsSource="{Binding ValidationErrors}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock
                                                Margin="0,2"
                                                FontSize="11"
                                                Foreground="PaleVioletRed"
                                                Text="{Binding}"
                                                TextWrapping="Wrap" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!--  Right Panel - Tools and Preview  -->
                <Border
                    Grid.Column="1"
                    MaxWidth="300"
                    Margin="10,0,0,0"
                    Style="{StaticResource ModernCardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionHeaderText}" Text="📋 Entry Tools" />

                        <!--  Quick Account Search  -->
                        <Border Margin="0,10,0,15" Style="{StaticResource DetailInfoBorder}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource DetailLabelText}" Text="Quick Account Search" />
                                <hc:TextBox
                                    Margin="0,5,0,0"
                                    Style="{StaticResource ModernTextBoxStyle}"
                                    Text="{Binding AccountSearchText, UpdateSourceTrigger=PropertyChanged}" />
                                <ListBox
                                    MaxWidth="280"
                                    MaxHeight="120"
                                    Margin="0,5,0,0"
                                    Background="#15FFFFFF"
                                    BorderBrush="#40FFFFFF"
                                    ItemsSource="{Binding FilteredAccounts}"
                                    SelectedItem="{Binding SelectedQuickAccount}"
                                    Visibility="{Binding HasAccountResults, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Padding="5" Background="Transparent">
                                                <Border.InputBindings>
                                                    <MouseBinding Command="{Binding DataContext.SelectQuickAccountCommand, RelativeSource={RelativeSource AncestorType=Page}}" MouseAction="LeftClick" />
                                                </Border.InputBindings>
                                                <TextBlock FontSize="11" Text="{Binding FullName}" />
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <Button
                                    Margin="0,5,0,0"
                                    Command="{Binding SelectQuickAccountCommand}"
                                    Content="Select Account"
                                    IsEnabled="{Binding SelectedQuickAccount, Converter={StaticResource NullToVisibilityConverter}}"
                                    Style="{StaticResource ModernSecondaryButton}"
                                    Visibility="{Binding HasAccountResults, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            </StackPanel>
                        </Border>

                        <!--  Common Templates  -->
                        <Border Margin="0,0,0,15" Style="{StaticResource DetailInfoBorder}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource DetailLabelText}" Text="Entry Templates" />
                                <ComboBox
                                    Margin="0,5,0,0"
                                    DisplayMemberPath="Name"
                                    ItemsSource="{Binding EntryTemplates}"
                                    SelectedItem="{Binding SelectedTemplate}"
                                    Style="{StaticResource ModernComboBox}" />
                                <StackPanel Margin="0,5,0,0" Orientation="Horizontal">
                                    <Button
                                        Margin="0,0,5,0"
                                        Command="{Binding ApplyTemplateCommand}"
                                        Content="Apply Template"
                                        IsEnabled="{Binding HasSelectedTemplate}"
                                        Style="{StaticResource ModernSecondaryButton}" />
                                    <Button
                                        Command="{Binding DeleteTemplateCommand}"
                                        Content="🗑️ Delete"
                                        IsEnabled="{Binding HasSelectedTemplate}"
                                        Style="{StaticResource ModernSecondaryButton}"
                                        ToolTip="Delete selected template" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!--  Recent Accounts  -->

                        <!--
                        <Border Style="{StaticResource DetailInfoBorder}" Visibility="{Binding HasValidationErrors, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock
                                    Foreground="PaleVioletRed"
                                    Style="{StaticResource DetailLabelText}"
                                    Text="⚠️ Validation Issues" />
                                <ItemsControl Margin="0,5,0,0" ItemsSource="{Binding ValidationErrors}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock
                                                Margin="0,2"
                                                FontSize="11"
                                                Foreground="PaleVioletRed"
                                                Text="{Binding}"
                                                TextWrapping="Wrap" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                        -->

                        <!--  Entry Preview  -->
                        <Border
                            Margin="0,15,0,0"
                            Style="{StaticResource DetailInfoBorder}"
                            Visibility="{Binding HasValidLines, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource DetailLabelText}" Text="📄 Entry Preview" />
                                <ScrollViewer
                                    MaxHeight="200"
                                    Margin="0,5,0,0"
                                    VerticalScrollBarVisibility="Auto">
                                    <TextBlock
                                        FontFamily="Consolas"
                                        FontSize="10"
                                        Foreground="White"
                                        Text="{Binding EntryPreview}"
                                        TextWrapping="Wrap" />
                                </ScrollViewer>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>

            <!--  Footer Actions  -->
            <Border Grid.Row="3" Style="{StaticResource ModernCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!--  Entry Summary  -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock
                            Margin="0,0,20,0"
                            VerticalAlignment="Center"
                            Style="{StaticResource FilterLabelText}"
                            Text="{Binding EntrySummary}" />
                        <Border Background="{Binding IsBalanced, Converter={StaticResource BalanceIndicatorConverter}}" CornerRadius="4">
                            <TextBlock
                                VerticalAlignment="Center"
                                FontSize="11"
                                FontWeight="Medium"
                                Foreground="Black"
                                Text="{Binding BalanceIndicator}" />
                        </Border>
                    </StackPanel>

                    <!--  Final Actions  -->
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button
                            Margin="0,0,10,0"
                            Command="{Binding ResetEntryCommand}"
                            Content="🔄 Reset"
                            Style="{StaticResource ModernSecondaryButton}" />
                        <Button
                            Margin="0,0,10,0"
                            Command="{Binding SaveTemplateCommand}"
                            Content="💾 Save as Template"
                            IsEnabled="{Binding CanSaveAsTemplate}"
                            Style="{StaticResource ModernSecondaryButton}" />
                        <Button
                            Command="{Binding PostEntryCommand}"
                            Content="✅ Post Entry"
                            IsEnabled="{Binding CanPost}"
                            Style="{StaticResource ModernActionButton}" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</base:BaseAnimatedPage>