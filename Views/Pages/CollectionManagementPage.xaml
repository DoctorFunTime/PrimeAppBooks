<base:BaseAnimatedPage
    x:Class="PrimeAppBooks.Views.Pages.CollectionManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:base="clr-namespace:PrimeAppBooks.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="800"
    d:DesignWidth="1200"
    Background="Transparent"
    mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <Border
            Grid.Row="0"
            Margin="0,0,0,20"
            Style="{StaticResource ModernCardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button
                    Grid.Column="0"
                    Height="Auto"
                    Margin="0,0,15,0"
                    Command="{Binding GoBackCommand}"
                    Content="◀"
                    FontSize="16"
                    Foreground="White"
                    Style="{StaticResource ModernIconButton}"
                    ToolTip="Go back" />

                <StackPanel Grid.Column="1">
                    <TextBlock Style="{StaticResource WelcomeTitleText}" Text="{Binding CustomerMetrics.CustomerName, FallbackValue='Customer Name'}" />
                    <TextBlock Style="{StaticResource WelcomeSubtitleText}" Text="Debt Collection Management and Payment Plans" />
                </StackPanel>

                <StackPanel
                    Grid.Column="2"
                    VerticalAlignment="Center"
                    Orientation="Horizontal">
                    <Border
                        Margin="0,0,10,0"
                        Padding="15,8"
                        Background="#1E1E1E"
                        CornerRadius="8">
                        <StackPanel>
                            <TextBlock
                                FontSize="10"
                                Foreground="#7F8C8D"
                                Text="TOTAL OUTSTANDING" />
                            <TextBlock
                                FontSize="16"
                                FontWeight="Bold"
                                Foreground="White"
                                Text="{Binding CustomerMetrics.TotalOutstanding, StringFormat=C}" />
                        </StackPanel>
                    </Border>
                    <Border
                        Padding="15,8"
                        Background="#1E1E1E"
                        CornerRadius="8">
                        <StackPanel>
                            <TextBlock
                                FontSize="10"
                                Foreground="#E74C3C"
                                Text="OVERDUE AMOUNT" />
                            <TextBlock
                                FontSize="16"
                                FontWeight="Bold"
                                Foreground="#E74C3C"
                                Text="{Binding CustomerMetrics.OverdueAmount, StringFormat=C}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!--  Action Cards  -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  New Follow-up Card  -->
            <Border
                Grid.Column="0"
                Margin="0,0,10,0"
                Style="{StaticResource ModernCardStyle}">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        Style="{StaticResource SectionHeaderText}"
                        Text="Log New Follow-up" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Style="{StaticResource FilterLabelText}" Text="Method" />
                            <ComboBox
                                Margin="0,5,0,10"
                                SelectedValue="{Binding NewFollowupMethod, UpdateSourceTrigger=PropertyChanged}"
                                SelectedValuePath="Content"
                                Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Phone" />
                                <ComboBoxItem Content="Email" />
                                <ComboBoxItem Content="SMS" />
                                <ComboBoxItem Content="Visit" />
                            </ComboBox>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                            <TextBlock Style="{StaticResource FilterLabelText}" Text="Follow-up Date" />
                            <DatePicker
                                Margin="0,5,0,10"
                                SelectedDate="{Binding NewFollowupDate}"
                                Style="{StaticResource CompactDatePickerStyle}" />
                        </StackPanel>
                    </Grid>
                    <TextBlock Style="{StaticResource FilterLabelText}" Text="Outcome / Promise to Pay" />
                    <hc:TextBox
                        Margin="0,5,0,10"
                        Style="{StaticResource ModernTextBoxStyle}"
                        Text="{Binding NewFollowupOutcome, UpdateSourceTrigger=PropertyChanged}" />
                    <Button
                        HorizontalAlignment="Right"
                        Command="{Binding AddFollowupCommand}"
                        Content="Add Follow-up Entry"
                        Style="{StaticResource ModernActionButton}" />
                </StackPanel>
            </Border>

            <!--  Create Plan Card  -->
            <Border
                Grid.Column="1"
                Margin="10,0,0,0"
                Style="{StaticResource ModernCardStyle}">
                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        Style="{StaticResource SectionHeaderText}"
                        Text="Create Payment Plan" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="80" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Style="{StaticResource FilterLabelText}" Text="Plan Name" />
                            <hc:TextBox
                                Margin="0,5,0,10"
                                Style="{StaticResource ModernTextBoxStyle}"
                                Text="{Binding NewPlanName, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="5,0,0,0">
                            <TextBlock Style="{StaticResource FilterLabelText}" Text="Installments" />
                            <hc:TextBox
                                Margin="0,5,0,10"
                                Style="{StaticResource ModernTextBoxStyle}"
                                Text="{Binding NumberOfInstallments, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Margin="0,0,5,0">
                            <TextBlock Style="{StaticResource FilterLabelText}" Text="Monthly Payment" />
                            <TextBlock
                                Margin="0,10"
                                FontSize="18"
                                FontWeight="Bold"
                                Foreground="#2ECC71"
                                Text="{Binding MonthlyInstallment, StringFormat=C}" />
                        </StackPanel>
                        <Button
                            Grid.Column="1"
                            VerticalAlignment="Bottom"
                            Command="{Binding CreatePaymentPlanCommand}"
                            Content="Activate Plan"
                            Style="{StaticResource ModernActionButton}" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <!--  History Content  -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  Interaction History  -->
            <Border
                Grid.Column="0"
                Margin="0,0,10,0"
                Style="{StaticResource ModernCardStyle}">
                <DockPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        DockPanel.Dock="Top"
                        Style="{StaticResource SectionHeaderText}"
                        Text="Collection Interaction History" />
                    <DataGrid
                        AutoGenerateColumns="False"
                        Background="Transparent"
                        BorderThickness="0"
                        CanUserAddRows="False"
                        GridLinesVisibility="None"
                        IsReadOnly="True"
                        ItemsSource="{Binding FollowupHistory}"
                        RowHeight="45"
                        Style="{StaticResource ModernDataGridStyle}">

                        <!--  Set row height and vertical alignment with enhanced selection state  -->
                        <DataGrid.RowStyle>
                            <Style BasedOn="{StaticResource MaterialDesignDataGridRow}" TargetType="DataGridRow">
                                <Setter Property="Height" Value="60" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter Property="Background" Value="Transparent" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#2A4A7B" />
                                        <!--  Selected background color  -->
                                        <Setter Property="BorderBrush" Value="#4472C4" />
                                        <Setter Property="BorderThickness" Value="1" />
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1E3A5F" />
                                        <!--  Hover color  -->
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <!--  Enhanced cell styling with selection state  -->
                        <DataGrid.CellStyle>
                            <Style BasedOn="{StaticResource MaterialDesignDataGridCell}" TargetType="DataGridCell">
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent" />
                                        <!--  Let row handle background  -->
                                        <Setter Property="BorderBrush" Value="Transparent" />
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>

                        <!--  Update column header style  -->
                        <DataGrid.ColumnHeaderStyle>
                            <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="DataGridColumnHeader">
                                <Setter Property="Foreground" Value="#FF4472C4" />
                                <Setter Property="FontWeight" Value="Bold" />
                                <Setter Property="FontFamily" Value="Segoe UI" />
                                <Setter Property="FontSize" Value="12" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Height" Value="35" />
                                <Setter Property="Background" Value="#1E1E1E" />
                                <Setter Property="BorderBrush" Value="#3A3A3A" />
                                <Setter Property="BorderThickness" Value="0,0,0,1" />
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn
                                Width="100"
                                Binding="{Binding FollowupDate, StringFormat=d}"
                                Header="DATE" />
                            <DataGridTextColumn
                                Width="100"
                                Binding="{Binding Method}"
                                Header="METHOD" />
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding Outcome}"
                                Header="OUTCOME" />
                            <DataGridTextColumn
                                Width="150"
                                Binding="{Binding Notes}"
                                Header="NOTES" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <!--  Active Plans  -->
            <Border
                Grid.Column="1"
                Margin="10,0,0,0"
                Style="{StaticResource ModernCardStyle}">
                <DockPanel>
                    <TextBlock
                        Margin="0,0,0,10"
                        DockPanel.Dock="Top"
                        Style="{StaticResource SectionHeaderText}"
                        Text="Active Payment Arrangements" />
                    <DataGrid
                        AutoGenerateColumns="False"
                        Background="Transparent"
                        BorderThickness="0"
                        CanUserAddRows="False"
                        GridLinesVisibility="None"
                        IsReadOnly="True"
                        ItemsSource="{Binding ActivePlans}"
                        RowHeight="45"
                        Style="{StaticResource ModernDataGridStyle}">

                        <!--  Set row height and vertical alignment with enhanced selection state  -->
                        <DataGrid.RowStyle>
                            <Style BasedOn="{StaticResource MaterialDesignDataGridRow}" TargetType="DataGridRow">
                                <Setter Property="Height" Value="60" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Setter Property="Foreground" Value="White" />
                                <Setter Property="Background" Value="Transparent" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#2A4A7B" />
                                        <!--  Selected background color  -->
                                        <Setter Property="BorderBrush" Value="#4472C4" />
                                        <Setter Property="BorderThickness" Value="1" />
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1E3A5F" />
                                        <!--  Hover color  -->
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <!--  Enhanced cell styling with selection state  -->
                        <DataGrid.CellStyle>
                            <Style BasedOn="{StaticResource MaterialDesignDataGridCell}" TargetType="DataGridCell">
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="Transparent" />
                                        <!--  Let row handle background  -->
                                        <Setter Property="BorderBrush" Value="Transparent" />
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>

                        <!--  Update column header style  -->
                        <DataGrid.ColumnHeaderStyle>
                            <Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="DataGridColumnHeader">
                                <Setter Property="Foreground" Value="#FF4472C4" />
                                <Setter Property="FontWeight" Value="Bold" />
                                <Setter Property="FontFamily" Value="Segoe UI" />
                                <Setter Property="FontSize" Value="12" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Height" Value="35" />
                                <Setter Property="Background" Value="#1E1E1E" />
                                <Setter Property="BorderBrush" Value="#3A3A3A" />
                                <Setter Property="BorderThickness" Value="0,0,0,1" />
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn
                                Width="*"
                                Binding="{Binding PlanName}"
                                Header="PLAN NAME" />
                            <DataGridTextColumn
                                Width="100"
                                Binding="{Binding NumberOfInstallments}"
                                Header="INSTALLMENTS" />
                            <DataGridTemplateColumn Width="120" Header="MONTHLY">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontWeight="Bold"
                                            Foreground="#2ECC71"
                                            Text="{Binding MonthlyInstallment, StringFormat=C}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn
                                Width="100"
                                Binding="{Binding StartDate, StringFormat=d}"
                                Header="STARTED" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</base:BaseAnimatedPage>