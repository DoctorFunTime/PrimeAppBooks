<Page
    x:Class="PrimeAppBooks.Views.Pages.ChartOfAccountsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="900"
    d:DesignWidth="1280"
    Background="Transparent"
    mc:Ignorable="d">

    <ScrollViewer Style="{StaticResource ModernScrollViewer}">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Header Section with Quick Actions  -->
            <Border
                Grid.Row="0"
                Margin="0,0,0,20"
                Style="{StaticResource ModernCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Style="{StaticResource WelcomeTitleText}" Text="Chart of Accounts" />
                        <TextBlock Style="{StaticResource WelcomeSubtitleText}" Text="Manage your accounting structure and account details" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button
                            Margin="5,0"
                            Command="{Binding AddAccountCommand}"
                            Content="+ New Account"
                            Style="{StaticResource ModernActionButton}" />
                        <Button
                            Margin="5,0"
                            Command="{Binding ImportAccountsCommand}"
                            Content="📥 Import"
                            Style="{StaticResource ModernSecondaryButton}" />
                        <Button
                            Margin="5,0"
                            Command="{Binding ExportAccountsCommand}"
                            Content="📊 Export"
                            Style="{StaticResource ModernSecondaryButton}" />
                    </StackPanel>
                </Grid>
            </Border>

            <!--  Account Summary Cards  -->
            <UniformGrid
                Grid.Row="1"
                Margin="0,0,0,20"
                Columns="4">
                <!--  Total Accounts  -->
                <Border Style="{StaticResource ModernCardStyle}">
                    <StackPanel Style="{StaticResource CardContentContainer}">
                        <TextBlock Style="{StaticResource DraftHeaderText}" Text="📊 Total Accounts" />
                        <TextBlock Style="{StaticResource CardValueText}" Text="{Binding TotalAccountsCount}" />
                        <TextBlock Style="{StaticResource CardSubText}" Text="All account types" />
                    </StackPanel>
                </Border>

                <!--  Assets  -->
                <Border Style="{StaticResource ModernCardStyle}">
                    <StackPanel Style="{StaticResource CardContentContainer}">
                        <TextBlock Style="{StaticResource PostedHeaderText}" Text="💼 Assets" />
                        <TextBlock Style="{StaticResource CardValueText}" Text="{Binding AssetAccountsCount}" />
                        <TextBlock Style="{StaticResource CardSubText}" Text="{Binding TotalAssets, StringFormat=C}" />
                    </StackPanel>
                </Border>

                <!--  Liabilities  -->
                <Border Style="{StaticResource ModernCardStyle}">
                    <StackPanel Style="{StaticResource CardContentContainer}">
                        <TextBlock Style="{StaticResource MonthlyHeaderText}" Text="📋 Liabilities" />
                        <TextBlock Style="{StaticResource CardValueText}" Text="{Binding LiabilityAccountsCount}" />
                        <TextBlock Style="{StaticResource CardSubText}" Text="{Binding TotalLiabilities, StringFormat=C}" />
                    </StackPanel>
                </Border>

                <!--  Equity  -->
                <Border Style="{StaticResource ModernCardStyle}">
                    <StackPanel Style="{StaticResource CardContentContainer}">
                        <TextBlock Style="{StaticResource BalanceHeaderText}" Text="⚖️ Equity" />
                        <TextBlock Style="{StaticResource CardValueText}" Text="{Binding EquityAccountsCount}" />
                        <TextBlock Style="{StaticResource CardSubText}" Text="{Binding TotalEquity, StringFormat=C}" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!--  Main Content Area  -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  Accounts Tree with Enhanced Features  -->
                <Border
                    Grid.Column="0"
                    Margin="0,0,10,0"
                    Style="{StaticResource ModernCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <!--  TreeView Header with Filters  -->
                        <Grid Grid.Row="0" Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="0"
                                Style="{StaticResource SectionHeaderText}"
                                Text="📑 Account Hierarchy" />

                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <ComboBox
                                    Width="120"
                                    Margin="0,0,10,0"
                                    ItemsSource="{Binding AccountTypeFilters}"
                                    SelectedValue="{Binding SelectedAccountTypeFilter}"
                                    Style="{StaticResource ModernComboBox}">
                                    <ComboBoxItem Content="All Types" />
                                    <ComboBoxItem Content="Assets" />
                                    <ComboBoxItem Content="Liabilities" />
                                    <ComboBoxItem Content="Equity" />
                                    <ComboBoxItem Content="Revenue" />
                                    <ComboBoxItem Content="Expenses" />
                                </ComboBox>
                                <Button
                                    Command="{Binding RefreshCommand}"
                                    Content="🔄"
                                    Style="{StaticResource ModernRefreshButton}" />
                            </StackPanel>
                        </Grid>

                        <!--  Enhanced TreeView  -->
                        <Border Grid.Row="1" Style="{StaticResource ModernDataGridContainer}">
                            <TreeView Foreground="White" ItemsSource="{Binding Accounts}">
                                <TreeView.Resources>
                                    <Style BasedOn="{StaticResource ModernScrollBarStyle}" TargetType="{x:Type ScrollBar}" />
                                </TreeView.Resources>

                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="30" />
                                                <ColumnDefinition Width="80" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="100" />
                                                <ColumnDefinition Width="100" />
                                                <ColumnDefinition Width="120" />
                                            </Grid.ColumnDefinitions>

                                            <!--  Account Type Indicator  -->
                                            <Border
                                                Grid.Column="0"
                                                Width="16"
                                                Height="16"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                Background="{Binding Type, Converter={StaticResource AccountTypeToColorConverter}}"
                                                CornerRadius="3">
                                                <Border.Effect>
                                                    <DropShadowEffect
                                                        BlurRadius="3"
                                                        Opacity="0.3"
                                                        ShadowDepth="1"
                                                        Color="Black" />
                                                </Border.Effect>
                                            </Border>

                                            <!--  Account Number  -->
                                            <TextBlock
                                                Grid.Column="1"
                                                VerticalAlignment="Center"
                                                FontFamily="Segoe UI"
                                                FontSize="13"
                                                FontWeight="Medium"
                                                Text="{Binding AccountNumber}"
                                                ToolTip="{Binding AccountNumber}" />

                                            <!--  Account Name  -->
                                            <TextBlock
                                                Grid.Column="2"
                                                VerticalAlignment="Center"
                                                FontFamily="Segoe UI"
                                                FontSize="13"
                                                Text="{Binding Name}"
                                                TextTrimming="CharacterEllipsis"
                                                ToolTip="{Binding Name}" />

                                            <!--  Account Type  -->
                                            <TextBlock
                                                Grid.Column="3"
                                                VerticalAlignment="Center"
                                                FontFamily="Segoe UI"
                                                FontSize="12"
                                                Foreground="#7F8C8D"
                                                Text="{Binding Type}"
                                                ToolTip="{Binding Type}" />

                                            <!--  Balance  -->
                                            <TextBlock
                                                Grid.Column="4"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                FontFamily="Segoe UI"
                                                FontSize="13"
                                                FontWeight="SemiBold"
                                                Foreground="{Binding Balance, Converter={StaticResource BalanceToColorConverter}}"
                                                Text="{Binding Balance, StringFormat=C}"
                                                ToolTip="{Binding Balance, StringFormat=C}" />

                                            <!--  Status Indicator  -->
                                            <StackPanel
                                                Grid.Column="5"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Orientation="Horizontal">
                                                <Border
                                                    Width="10"
                                                    Height="10"
                                                    Margin="0,0,5,0"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Background="{Binding IsActive, Converter={StaticResource BooleanToStatusColorConverter}}"
                                                    CornerRadius="5">
                                                    <Border.Effect>
                                                        <DropShadowEffect
                                                            BlurRadius="3"
                                                            Opacity="0.3"
                                                            ShadowDepth="1"
                                                            Color="Black" />
                                                    </Border.Effect>
                                                </Border>
                                                <TextBlock
                                                    FontFamily="Segoe UI"
                                                    FontSize="12"
                                                    Foreground="#7F8C8D"
                                                    Text="{Binding IsActive, Converter={StaticResource BooleanToStatusTextConverter}}" />
                                            </StackPanel>
                                        </Grid>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>

                                <TreeView.ItemContainerStyle>
                                    <Style TargetType="{x:Type TreeViewItem}">
                                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                        <Setter Property="FontWeight" Value="Normal" />
                                        <Style.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="FontWeight" Value="Bold" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TreeView.ItemContainerStyle>
                            </TreeView>
                        </Border>
                    </Grid>
                </Border>

                <!--  Account Details Panel  -->
                <Border
                    Grid.Column="1"
                    Margin="10,0,0,0"
                    Style="{StaticResource ModernCardStyle}">
                    <StackPanel>
                        <TextBlock Style="{StaticResource SectionHeaderText}" Text="📋 Account Details" />

                        <!--  Account Details (shown when account selected)  -->
                        <StackPanel Margin="0,10,0,0" Visibility="{Binding SelectedAccount, Converter={StaticResource NullToVisibilityConverter}}">
                            <!--  Account Header Info  -->
                            <Border Margin="0,0,0,10" Style="{StaticResource DetailInfoBorder}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource DetailHeaderText}" Text="{Binding SelectedAccount.Name}" />
                                    <TextBlock Style="{StaticResource DetailSubText}" Text="{Binding SelectedAccount.AccountNumber}" />
                                    <Border
                                        Margin="0,5,0,0"
                                        HorizontalAlignment="Left"
                                        Background="{Binding SelectedAccount.Type, Converter={StaticResource AccountTypeToColorConverter}}"
                                        Style="{StaticResource TransactionStatusBadge}">
                                        <TextBlock Style="{StaticResource StatusBadgeText}" Text="{Binding SelectedAccount.Type}" />
                                    </Border>
                                </StackPanel>
                            </Border>

                            <!--  Account Details  -->
                            <Border Margin="0,0,0,10" Style="{StaticResource DetailInfoBorder}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource DetailLabelText}" Text="Description" />
                                    <TextBlock
                                        Style="{StaticResource DetailValueText}"
                                        Text="{Binding SelectedAccount.Description}"
                                        TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>

                            <!--  Balance Information  -->
                            <Border Margin="0,0,0,10" Style="{StaticResource DetailInfoBorder}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource DetailLabelText}" Text="Balance" />
                                    <TextBlock
                                        Foreground="{Binding SelectedAccount.Balance, Converter={StaticResource BalanceToColorConverter}}"
                                        Style="{StaticResource DetailValueText}"
                                        Text="{Binding SelectedAccount.Balance, StringFormat=C}" />
                                </StackPanel>
                            </Border>

                            <!--  Status Information  -->
                            <Border Margin="0,0,0,10" Style="{StaticResource DetailInfoBorder}">
                                <StackPanel>
                                    <TextBlock Style="{StaticResource DetailLabelText}" Text="Status" />
                                    <StackPanel Orientation="Horizontal">
                                        <Border
                                            Width="10"
                                            Height="10"
                                            Margin="0,0,5,0"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center"
                                            Background="{Binding SelectedAccount.IsActive, Converter={StaticResource BooleanToStatusColorConverter}}"
                                            CornerRadius="5" />
                                        <TextBlock Style="{StaticResource DetailValueText}" Text="{Binding SelectedAccount.IsActive, Converter={StaticResource BooleanToStatusTextConverter}}" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!--  Quick Actions for Selected Account  -->
                            <StackPanel Margin="0,10,0,0">
                                <Button
                                    Margin="0,0,0,5"
                                    Command="{Binding EditAccountCommand}"
                                    CommandParameter="{Binding SelectedAccount}"
                                    Content="📝 Edit Account"
                                    Style="{StaticResource ModernSecondaryButton}" />
                                <Button
                                    Margin="0,0,0,5"
                                    Command="{Binding ViewTransactionsCommand}"
                                    CommandParameter="{Binding SelectedAccount}"
                                    Content="📋 View Transactions"
                                    Style="{StaticResource ModernSecondaryButton}" />
                                <Button
                                    Margin="0,0,0,5"
                                    Command="{Binding ToggleAccountStatusCommand}"
                                    CommandParameter="{Binding SelectedAccount}"
                                    Content="{Binding SelectedAccount.IsActive, Converter={StaticResource ToggleStatusTextConverter}}"
                                    Style="{StaticResource ModernSecondaryButton}" />
                            </StackPanel>
                        </StackPanel>

                        <!--  Empty State  -->
                        <StackPanel
                            Margin="0,20,0,0"
                            HorizontalAlignment="Center"
                            Visibility="{Binding SelectedAccount, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Invert}">
                            <TextBlock
                                Margin="0,0,0,10"
                                HorizontalAlignment="Center"
                                FontSize="48"
                                Opacity="0.3"
                                Text="📑" />
                            <TextBlock
                                HorizontalAlignment="Center"
                                Style="{StaticResource DetailSubText}"
                                Text="Select an account to view details" />
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>